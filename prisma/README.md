# Legacy Prisma schema (archived)

KellyOS now uses **Firebase Firestore** as the backend data store.

See:
- `src/lib/db.ts` — Firestore / demo document store
- `src/lib/firebase.ts` — Firebase configuration
- `scripts/seed-firebase.ts` — seed script
- `.env.example` — Firebase env vars

This folder is retained only as historical reference and is excluded from TypeScript compilation.
